# 飞书应用凭证
FEISHU_APP_ID=your_app_id
FEISHU_APP_SECRET=your_app_secret

# 飞书多维表格配置
FEISHU_BASE_APP_TOKEN=D6JAbvNKZaUgMGsTfkPcgXn2nBd
FEISHU_TABLE_ID=tbluFzEQv1KRMsiG

# ===== 浏览器模式 =====
# login   — 打开浏览器让用户手动登录（默认）
# profile — 复制本机 Chrome 登录态（需本机已登录淘宝）
# cdp     — 连接已开启 --remote-debugging-port 的 Chrome
BROWSER_MODE=login

# profile 模式: Chrome 用户数据目录（留空则自动检测）
# Windows:  C:\Users\<用户名>\AppData\Local\Google\Chrome\User Data
# macOS:    ~/Library/Application Support/Google/Chrome
CHROME_USER_DATA_DIR=

# cdp 模式: Chrome 调试端口（使用 cdp 时把上一行 BROWSER_MODE 改为 cdp）
# BROWSER_MODE=cdp
CHROME_DEBUG_PORT=9222

# 监控间隔（秒）
MONITOR_INTERVAL=10

# 评论检查范围（分钟）
COMMENT_CHECK_MINUTES=5

# 首次同步是否滚动评论列表（默认 false，直播进行中建议保持 false）
# SCROLL_ON_SYNC=true

# 启动兜底：程序打开时直播已在进行，先全量扫描历史评论并落盘（默认 true）
# STARTUP_BACKFILL=false

# 定时滚动兜底间隔（小时），衔接虚拟列表防止漏采；0 关闭，默认 3
PERIODIC_BACKFILL_HOURS=3
